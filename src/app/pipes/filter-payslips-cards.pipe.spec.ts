import { FilterPayslipsCardsPipe } from './filter-payslips-cards.pipe';

describe('FilterPayslipsCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPayslipsCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
