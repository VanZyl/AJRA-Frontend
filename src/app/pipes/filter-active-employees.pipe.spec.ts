import { FilterActiveEmployeesPipe } from './filter-active-employees.pipe';

describe('FilterActiveEmployeesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActiveEmployeesPipe();
    expect(pipe).toBeTruthy();
  });
});
