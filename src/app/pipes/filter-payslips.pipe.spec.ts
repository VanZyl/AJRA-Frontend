import { FilterPayslipsPipe } from './filter-payslips.pipe';

describe('FilterPayslipsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPayslipsPipe();
    expect(pipe).toBeTruthy();
  });
});
